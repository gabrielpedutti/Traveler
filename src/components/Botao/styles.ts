import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  botao: {
    marginTop: 25,
    marginStart: 15,
    marginEnd: 15,
    backgroundColor: '#2c88d9',
    alignItems: 'center',
    padding: 15,
    borderRadius: 5
  },
  textoBotao: {
    color: '#fff',
    textAlign: 'center',
    fontWeight: 'bold',
    fontSize: 15,
  },
})

export { styles };