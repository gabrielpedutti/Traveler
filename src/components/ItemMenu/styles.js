import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    margin: 10,
    marginBottom: 20,
    marginTop: 20,
    padding: 5,
  },
  label: {
    color: "#fff",
    fontWeight: "bold",
    fontSize: 25,
    marginLeft: 15,
  }
})

export { styles };