import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    marginTop: 20,
  },
  label: {
    color: "#fff",
    fontWeight: "bold",
    fontSize: 35,
    textAlign: 'center',
  }
})

export { styles };