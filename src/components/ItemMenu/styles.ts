import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    padding: 25,
    backgroundColor: '#3d97e6a9',
    width: '100%',
  },
  label: {
    color: "#fff",
    fontWeight: "bold",
    fontSize: 25,
    marginLeft: 15,
  }
})

export { styles };